import {BackgroudChangeDirective} from "./backgroud-change.directive";

describe('BackgroundChangeDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new BackgroudChangeDirective();
    expect(directive).toBeTruthy();
  });
});
